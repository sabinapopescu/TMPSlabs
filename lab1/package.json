{
    "name": "creational-lab-bloomify",
    "version": "1.0.0",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "tsc -p .",
        "dev": "tsc -w",
        "preview": "http-server public -p 8080 -c-1 --cors",
        "preview:open": "http-server public -p 8080 -c-1 --cors & sleep 1 && open http://localhost:8080"
    },
    "devDependencies": {
        "http-server": "^14.1.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.3"
    }
}
